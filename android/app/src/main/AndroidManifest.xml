<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required in release builds too: the Flutter tool only adds INTERNET to
         the debug and profile manifests. PayPaw needs it to reach Supabase and
         to fetch the Inter typeface on first launch. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Bill reminders.

         POST_NOTIFICATIONS is a runtime permission from Android 13 (API 33).
         Below that, notifications are allowed by default and this is inert.

         RECEIVE_BOOT_COMPLETED lets the receiver below restore scheduled
         reminders after a restart. Android drops every pending alarm on reboot,
         so without this a phone restarted on the 3rd silently loses every
         reminder that had been set for the 5th - and nothing tells the user.

         **No SCHEDULE_EXACT_ALARM, and no USE_EXACT_ALARM.** Google Play
         restricts both to apps whose *core* function is alarms or calendars,
         and a bills tracker is neither; declaring one risks the listing. The
         cost is that reminders are scheduled inexactly and may drift by minutes
         in Doze. A reminder that a bill is due today arriving at 9:14 instead of
         9:00 loses nothing. An alarm clock that did the same would be broken -
         which is exactly the distinction the policy is drawing. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:label="PayPaw"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Auth deep links. Supabase sends the user back here after an
                 email confirmation, a password reset, or an OAuth sign-in.
                 The scheme matches the application ID so it cannot collide with
                 another app on the device, and the same URL must be listed under
                 Authentication > URL Configuration > Redirect URLs in the
                 Supabase dashboard - see AppConfig.authRedirectUrl. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="com.paypaw.app"
                    android:host="login-callback"/>
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications' receivers.
             https://pub.dev/packages/flutter_local_notifications#-android-setup

             The plugin ships these classes; they are declared here because a
             manifest cannot register a receiver it has not been told about.

             ScheduledNotificationReceiver is what actually posts a reminder when
             its alarm fires. ScheduledNotificationBootReceiver re-registers the
             pending alarms after a reboot or an app update, both of which clear
             them. Without the second one every reminder set before a restart
             simply never arrives. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
